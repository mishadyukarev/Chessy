namespace Assets.Scripts.ECS.Components
{
    internal struct PlayersInfoComponent
    {
        //private Dictionary<bool, Player> _playersByBoolDict;


        //internal void StartFill()
        //{
        //    _playersByBoolDict = new Dictionary<bool, Player>();
        //}

        //internal void AddPlayerByBool(Player player) => _playersByBoolDict.Add(player.IsMasterClient, player);
        ////internal Player GetPlayerByBool(bool key) => _playersByBoolDict[key];
        //internal int GetActorNumber(bool keyIsMaster) => _playersByBoolDict[keyIsMaster].ActorNumber;
    }
}
